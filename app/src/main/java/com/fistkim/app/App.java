/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.fistkim.app;

import com.fistkim.list.LinkedList;

import static com.fistkim.utilities.StringUtils.join;
import static com.fistkim.utilities.StringUtils.split;
import static com.fistkim.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
